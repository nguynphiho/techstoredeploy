export const productCarts = (state) => state.cartReducer.productCart;
export const isOpenSelector = (state) => state.cartReducer.isOpen;
export const isOpenSnackbar = (state) => state.cartReducer.notify;